package setup1.Interfaces.wonpackage;

/**
 * The elements of an interface are public but the interface itself is not,
 * until we declare it public for use outside its package:
 */

public interface AnInterface {
    void f();

    void g();

    void h();
}
